//Convert a string to an array of strings
// convert strings into seperate strings in an array
function stringToArray(string) {
  let str = string.split(" ");

  return str;
} console.log(stringToArray("I love arrays they are my favorite"));
