//  divide first number
function isDivideBy(number, a, b) {
  number % a === 0 && number % b === 0 ? true : false
                    // OR
// if (number % a === 0 && number % b === 0) {
//     return true
// } else {
//     return false
// }
}